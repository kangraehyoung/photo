<?xml version="1.0" encoding="UTF-8"?> <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rh.photo.dao.MemberMapper">
	<insert id="insertMember" parameterType="HashMap">
	insert into member(name, id, email, password) values (#{name}, #{userId}, #{email}, #{password})
	</insert>
	
	<select id="memberExistChk" parameterType="HashMap" resultType="_int">
	select exists (select no from member where id = #{userId} and password = #{password})
	</select>
	
	<select id="getMemberById" parameterType="HashMap" resultType="HashMap">
	select no, id, email, name from member where id = #{userId}
	</select>
	
	<delete id="deleteMemberByNo" parameterType="HashMap">
	delete from member where no = #{no}
	</delete>
</mapper>